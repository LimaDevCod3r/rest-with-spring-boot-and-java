spring:
  application:
    name: api-rest-spring-boot
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_with_spring_boot_and_java?useTimezone=true&serverTimezone=UTC
    username: root
    password: admin123
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    show-sql: false
logging:
  level:
    #  root: WARN # logging de WARN todo o projeto
    br.LimaDevCod3r: DEBUG # logging de DEBUG apenas nesse pacote